language: haskell
ghc:
  - 7.6
  - 7.8

before_install:
  - travis_retry sudo apt-get update -qq
  - travis_retry sudo apt-get install -qq libgsl0-dev liblapack-dev

install:
  - cabal update
  - cabal install -j2 --only-dependencies --enable-tests --enable-benchmarks
  - git clone git://github.com/ekmett/algebra.git
  - cd algebra
  - git checkout euclidean
  - cabal install ./algebra.cabal
  - cd ../

script:
  - cabal configure --enable-tests --enable-benchmarks -v2  # -v2 provides useful information for debugging
  - cabal build   # this builds all libraries and executables (including tests/benchmarks)
  - cabal test
  - cabal check
  - cabal sdist   # tests that a source-distribution can be generated
 
  # The following scriptlet checks that the resulting source distribution can be built & installed
  - export SRC_TGZ=$(cabal info . | awk '{print $2 ".tar.gz";exit}') ;
    cd dist/;
    if [ -f "$SRC_TGZ" ]; then
       cabal install "$SRC_TGZ";
    else
       echo "expected '$SRC_TGZ' not found";
       exit 1;
    fi
 
matrix:
  allow_failures:
    - ghc: 
        - 7.8
